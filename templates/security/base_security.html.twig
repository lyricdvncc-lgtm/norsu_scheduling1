<!DOCTYPE html>
<html lang="en" class="h-full">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Smart Scheduling System{% endblock %}</title>
    <link rel="icon" type="image/svg+xml" href="{{ asset('images/norsu-logo.svg') }}">
    
    <!-- Tailwind CSS -->
    <link href="/build/app.css" rel="stylesheet">
    
    {{ importmap('app') }}
    
    {% block stylesheets %}
    <style>
        @layer utilities {
            .gradient-bg {
                background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            }
        }
    </style>
    {% endblock %}
</head>
<body class="h-full bg-gray-50">
    <div class="min-h-screen flex justify-center items-center py-8 px-4 sm:px-6 lg:px-8">
        <div class="max-w-6xl w-full bg-white shadow-xl rounded-2xl overflow-hidden flex flex-col lg:flex-row">
            <!-- Left Side - Form Content -->
            <div class="w-full lg:w-1/2 p-8 sm:p-12 lg:p-16">
                {% block form_content %}
                {% endblock %}
            </div>
            
            <!-- Right Side - Image/Branding -->
            <div class="w-full lg:w-1/2 bg-gradient-to-br from-indigo-600 to-purple-700 text-white hidden lg:flex">
                <div class="p-12 w-full flex flex-col items-center justify-center">
                    {% block side_content %}
                    <div class="mb-8">
                        <img src="{{ asset('images/norsu-logo.svg') }}" alt="NORSU Logo" class="w-24 h-24 mx-auto object-contain filter brightness-0 invert">
                    </div>
                    <h2 class="text-3xl font-bold mb-3 text-center">Negros Oriental State University</h2>
                    <p class="text-xl font-medium mb-6 text-indigo-100">Smart Scheduling System</p>
                    <p class="text-indigo-100 max-w-md text-center leading-relaxed">
                        Streamline your academic scheduling with our intelligent system designed for efficient faculty and resource management.
                    </p>
                    <div class="mt-12 flex space-x-2">
                        <div class="w-2 h-2 rounded-full bg-white opacity-75"></div>
                        <div class="w-2 h-2 rounded-full bg-white opacity-50"></div>
                        <div class="w-2 h-2 rounded-full bg-white opacity-25"></div>
                    </div>
                    {% endblock %}
                </div>
            </div>
        </div>
    </div>
    
    {% block javascripts %}
    {% endblock %}
</body>
</html>
