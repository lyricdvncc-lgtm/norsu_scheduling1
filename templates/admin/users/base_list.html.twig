{% extends 'admin/base.html.twig' %}

{% block title %}{{ page_title }}{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <!-- DataTables CSS - Local -->
    <link rel="stylesheet" href="{{ asset('vendor/css/jquery.dataTables.min.css') }}">
    <style>
        /* Override DataTables default row striping (uses box-shadow on cells) */
        table.dataTable tbody tr {
            background-color: #ffffff !important;
        }
        table.dataTable tbody tr > * {
            box-shadow: none !important;
        }
        table.dataTable tbody tr:hover {
            background-color: #f9fafb !important;
        }
        /* Hide DataTables filter label text */
        .dataTables_filter label {
            font-size: 0;
        }
        .dataTables_filter label input {
            font-size: 0.875rem;
        }
        /* Hide DataTables length control */
        .dataTables_length {
            display: none !important;
        }
    </style>
{% endblock %}

{% block main_content %}
    <!-- Page Header -->
    <div class="mb-8">
        <div class="flex flex-col lg:flex-row lg:items-center lg:justify-between">
            <div class="flex-1 min-w-0">
                <h1 class="text-3xl font-bold text-gray-900 sm:text-4xl">{{ page_title }}</h1>
                {% if user_type_description is defined %}
                    <p class="mt-2 text-sm text-gray-500">{{ user_type_description }}</p>
                {% endif %}
            </div>
            {% block page_actions %}
            <div class="mt-5 flex lg:mt-0 lg:ml-4">
                {% if user_type == 'all' %}
                    <a href="{{ path('admin_users_create') }}" class="inline-flex items-center px-6 py-3 border border-transparent text-base font-medium rounded-lg shadow-sm text-white bg-gradient-to-r from-blue-600 to-blue-700 hover:from-blue-700 hover:to-blue-800">
                        <svg class="-ml-1 mr-2 h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4"/>
                        </svg>
                        Add New User
                    </a>
                {% endif %}
            </div>
            {% endblock %}
        </div>
    </div>

<!-- Statistics Cards -->
<div class="grid grid-cols-1 md:grid-cols-4 gap-6 mb-8">
    {% block stats_cards %}
    <div class="bg-white rounded-2xl shadow-lg p-6 border border-gray-100">
        <div class="flex items-center">
            <div class="flex-shrink-0">
                <div class="w-12 h-12 bg-gradient-to-br from-blue-500 to-blue-600 rounded-xl flex items-center justify-center">
                    {% block stat_icon %}
                    <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4.354a4 4 0 110 5.292M15 21H3v-1a6 6 0 0112 0v1zm0 0h6v-1a6 6 0 00-9-5.197m13.5-9a2.5 2.5 0 11-5 0 2.5 2.5 0 015 0z"/>
                    </svg>
                    {% endblock %}
                </div>
            </div>
            <div class="ml-4 flex-1">
                <p class="text-sm font-medium text-gray-600">{{ user_type == 'all' ? 'Total Users' : page_title }}</p>
                <p class="text-2xl font-bold text-gray-900">{{ pagination.total }}</p>
            </div>
        </div>
    </div>
    
    <div class="bg-white rounded-2xl shadow-lg p-6 border border-gray-100">
        <div class="flex items-center">
            <div class="flex-shrink-0">
                <div class="w-12 h-12 bg-gradient-to-br from-green-500 to-green-600 rounded-xl flex items-center justify-center">
                    <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/>
                    </svg>
                </div>
            </div>
            <div class="ml-4 flex-1">
                <p class="text-sm font-medium text-gray-600">Active</p>
                <p class="text-2xl font-bold text-gray-900">{{ users|filter(u => u.isActive)|length }}</p>
            </div>
        </div>
    </div>
    
    <div class="bg-white rounded-2xl shadow-lg p-6 border border-gray-100">
        <div class="flex items-center">
            <div class="flex-shrink-0">
                <div class="w-12 h-12 bg-gradient-to-br from-yellow-500 to-yellow-600 rounded-xl flex items-center justify-center">
                    <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M18.364 18.364A9 9 0 005.636 5.636m12.728 12.728L5.636 5.636m12.728 12.728L18.364 5.636M5.636 18.364l12.728-12.728"/>
                    </svg>
                </div>
            </div>
            <div class="ml-4 flex-1">
                <p class="text-sm font-medium text-gray-600">Inactive</p>
                <p class="text-2xl font-bold text-gray-900">{{ users|filter(u => not u.isActive)|length }}</p>
            </div>
        </div>
    </div>
    
    <div class="bg-white rounded-2xl shadow-lg p-6 border border-gray-100">
        <div class="flex items-center">
            <div class="flex-shrink-0">
                <div class="w-12 h-12 bg-gradient-to-br from-purple-500 to-purple-600 rounded-xl flex items-center justify-center">
                    <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"/>
                    </svg>
                </div>
            </div>
            <div class="ml-4 flex-1">
                <p class="text-sm font-medium text-gray-600">Recent Logins</p>
                <p class="text-2xl font-bold text-gray-900">{{ users|filter(u => u.lastLogin and u.lastLogin.format('U') > date('-7 days').format('U'))|length }}</p>
                <p class="text-xs text-gray-500 mt-1">Last 7 days</p>
            </div>
        </div>
    </div>
    {% endblock %}
</div>
<div class="bg-white rounded-2xl shadow-lg border border-gray-100 overflow-hidden">
    <!-- Filters Bar -->
    <div class="px-6 py-4 border-b border-gray-200 bg-gray-50">
        <div class="flex items-center justify-between gap-4">
            <h3 class="text-lg font-semibold text-gray-900">{{ page_title }} Directory</h3>
            
            <div class="flex flex-wrap items-center gap-3" id="filtersContainer">
                <!-- Search will be inserted here by JavaScript -->

                {% if user_type == 'all' %}
                    <!-- Role Filter -->
                    <div class="filter-group">
                        <select id="roleFilter" name="role" class="w-36 rounded-lg border-gray-300 shadow-sm focus:border-indigo-500 focus:ring-indigo-500 text-sm py-2 px-3 pr-8 bg-white hover:bg-gray-50 transition-colors">
                            <option value="">All Roles</option>
                            <option value="Administrator" {{ (filters.role|default(null)) == 1 ? 'selected' : '' }}>Administrator</option>
                            <option value="Department Head" {{ (filters.role|default(null)) == 2 ? 'selected' : '' }}>Department Head</option>
                            <option value="Faculty" {{ (filters.role|default(null)) == 3 ? 'selected' : '' }}>Faculty</option>
                        </select>
                    </div>
                {% endif %}
                
                <!-- Status Filter -->
                <div class="filter-group">
                    <select id="statusFilter" name="is_active" class="w-32 rounded-lg border-gray-300 shadow-sm focus:border-indigo-500 focus:ring-indigo-500 text-sm py-2 px-3 pr-8 bg-white hover:bg-gray-50 transition-colors">
                        <option value="" selected>All Status</option>
                        <option value="Active">Active</option>
                        <option value="Inactive">Inactive</option>
                    </select>
                </div>

                <!-- College Filter -->
                {% if user_type in ['all', 'department_heads', 'faculty'] %}
                <div class="filter-group">
                    <select id="collegeFilter" name="college_id" class="w-40 rounded-lg border-gray-300 shadow-sm focus:border-indigo-500 focus:ring-indigo-500 text-sm py-2 px-3 pr-8 bg-white hover:bg-gray-50 transition-colors">
                        <option value="">All Colleges</option>
                        {% if colleges is defined %}
                            {% for college in colleges %}
                                <option value="{{ college.id }}" {{ (filters.college_id|default(null)) == college.id ? 'selected' : '' }}>
                                    {{ college.name }}
                                </option>
                            {% endfor %}
                        {% endif %}
                    </select>
                </div>
                {% endif %}

                <!-- Department Filter -->
                {% if user_type in ['all', 'department_heads', 'faculty'] %}
                <div class="filter-group">
                    <select id="departmentFilter" name="department_id" class="w-48 rounded-lg border-gray-300 shadow-sm focus:border-indigo-500 focus:ring-indigo-500 text-sm py-2 px-3 pr-8 bg-white hover:bg-gray-50 transition-colors">
                        <option value="">All Departments</option>
                        {% if departments is defined %}
                            {% for department in departments %}
                                <option value="{{ department.id }}" 
                                        data-college-id="{{ department.college.id }}"
                                        {{ (filters.department_id|default(null)) == department.id ? 'selected' : '' }}>
                                    {{ department.name }}
                                </option>
                            {% endfor %}
                        {% endif %}
                    </select>
                </div>
                {% endif %}

                <!-- Clear Filters Button -->
                <button id="clearFilters" type="button" class="inline-flex items-center px-3 py-2 border border-gray-300 shadow-sm text-sm leading-4 font-medium rounded-lg text-gray-700 bg-white hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500 transition-colors">
                    <svg class="-ml-0.5 mr-2 h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/>
                    </svg>
                    Clear
                </button>
            </div>
        </div>
    </div>

    <!-- Table Content -->
    <div class="overflow-x-auto">
        <table id="usersTable" class="min-w-full divide-y divide-gray-200">
            <thead class="bg-gray-50">
                <tr>
                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Username</th>
                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Name</th>
                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Email</th>
                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Employee ID</th>
                    {% if user_type == 'all' %}
                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Role</th>
                    {% endif %}
                    {% if user_type in ['all', 'department_heads', 'faculty'] %}
                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Department</th>
                    {% endif %}
                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Status</th>
                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Actions</th>
                </tr>
            </thead>
            <tbody class="bg-white divide-y divide-gray-200">
                {% for user in users %}
                <tr class="hover:bg-gray-50">
                    <td class="px-6 py-4 whitespace-nowrap text-sm font-medium text-gray-900">{{ user.username }}</td>
                    <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900">{{ user.fullName ?: '-' }}</td>
                    <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900">{{ user.email }}</td>
                    <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900">{{ user.employeeId ?: '-' }}</td>
                    {% if user_type == 'all' %}
                    <td class="px-6 py-4 whitespace-nowrap">
                        <span class="inline-flex px-2 py-1 text-xs font-semibold rounded-full {{ user.role == 1 ? 'bg-red-100 text-red-800' : (user.role == 2 ? 'bg-yellow-100 text-yellow-800' : 'bg-blue-100 text-blue-800') }}">
                            {{ user.roleDisplayName }}
                        </span>
                    </td>
                    {% endif %}
                    {% if user_type in ['all', 'department_heads', 'faculty'] %}
                    <td class="px-6 py-4 text-sm text-gray-900">
                        {% if user.department %}
                            <div class="truncate max-w-xs" title="{{ user.department.name }}">{{ user.department.name }}</div>
                        {% else %}
                            <span class="text-gray-400">-</span>
                        {% endif %}
                    </td>
                    {% endif %}
                    <td class="px-6 py-4 whitespace-nowrap">
                        {% if user.isActive %}
                            <span class="inline-flex px-2 py-1 text-xs font-semibold rounded-full bg-green-100 text-green-800">Active</span>
                        {% else %}
                            <span class="inline-flex px-2 py-1 text-xs font-semibold rounded-full bg-gray-100 text-gray-800">Inactive</span>
                        {% endif %}
                    </td>
                    <td class="px-6 py-4 whitespace-nowrap text-sm font-medium">
                        <div class="flex items-center gap-2">
                            <a href="{{ path('admin_users_view', {id: user.id}) }}" 
                               class="text-indigo-600 hover:text-indigo-900 inline-flex items-center justify-center w-8 h-8 rounded-lg hover:bg-indigo-50 transition" 
                               title="View">
                                <svg class="h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"/>
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z"/>
                                </svg>
                            </a>
                            {% if user.role == 3 %}
                            <a href="{{ path('admin_users_faculty_history', {id: user.id}) }}" 
                               class="text-purple-600 hover:text-purple-900 inline-flex items-center justify-center w-8 h-8 rounded-lg hover:bg-purple-50 transition" 
                               title="Teaching History">
                                <svg class="h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"/>
                                </svg>
                            </a>
                            {% endif %}
                            <a href="{{ path('admin_users_edit', {id: user.id, return_url: app.request.uri}) }}" 
                               class="text-gray-600 hover:text-gray-900 inline-flex items-center justify-center w-8 h-8 rounded-lg hover:bg-gray-100 transition" 
                               title="Edit">
                                <svg class="h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z"/>
                                </svg>
                            </a>
                            {% if user.isActive %}
                                <form method="POST" action="{{ path('admin_users_deactivate', {id: user.id}) }}" 
                                      class="inline m-0">
                                    <input type="hidden" name="_token" value="{{ csrf_token('deactivate_user_' ~ user.id) }}">
                                    <button type="submit" 
                                            class="text-yellow-600 hover:text-yellow-900 inline-flex items-center justify-center w-8 h-8 rounded-lg hover:bg-yellow-50 transition" 
                                            title="Deactivate" 
                                            onclick="return confirm('Are you sure you want to deactivate this user?')">
                                        <svg class="h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M18.364 18.364A9 9 0 005.636 5.636m12.728 12.728L5.636 5.636m12.728 12.728L18.364 5.636M5.636 18.364l12.728-12.728"/>
                                        </svg>
                                    </button>
                                </form>
                            {% else %}
                                <form method="POST" action="{{ path('admin_users_activate', {id: user.id}) }}" 
                                      class="inline m-0">
                                    <input type="hidden" name="_token" value="{{ csrf_token('activate_user_' ~ user.id) }}">
                                    <button type="submit" 
                                            class="text-green-600 hover:text-green-900 inline-flex items-center justify-center w-8 h-8 rounded-lg hover:bg-green-50 transition" 
                                            title="Activate">
                                        <svg class="h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/>
                                        </svg>
                                    </button>
                                </form>
                            {% endif %}
                            <form method="POST" action="{{ path('admin_users_delete', {id: user.id}) }}" 
                                  class="inline m-0">
                                <input type="hidden" name="_token" value="{{ csrf_token('delete_user_' ~ user.id) }}">
                                <button type="submit" 
                                        class="text-red-600 hover:text-red-900 inline-flex items-center justify-center w-8 h-8 rounded-lg hover:bg-red-50 transition" 
                                        title="Delete" 
                                        onclick="return confirm('Are you sure you want to delete this user? This action cannot be undone.')">
                                    <svg class="h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16"/>
                                    </svg>
                                </button>
                            </form>
                        </div>
                    </td>
                </tr>
                {% else %}
                <tr>
                    <td colspan="{% if user_type == 'all' %}8{% else %}7{% endif %}" class="px-6 py-12 text-center">
                        <svg class="mx-auto h-12 w-12 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4.354a4 4 0 110 5.292M15 21H3v-1a6 6 0 0112 0v1zm0 0h6v-1a6 6 0 00-9-5.197m13.5-9a2.5 2.5 0 11-5 0 2.5 2.5 0 015 0z"/>
                        </svg>
                        <h3 class="mt-2 text-sm font-medium text-gray-900">No {{ page_title|lower }} found</h3>
                        <p class="mt-1 text-sm text-gray-500">{% if user_type == 'all' %}Get started by creating a new user.{% else %}No {{ page_title|lower }} match your current filters.{% endif %}</p>
                    </td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>

    <!-- Old pagination removed - DataTables handles pagination now -->
</div>

{% block javascripts %}
{{ parent() }}
<!-- jQuery (required for DataTables) - Local -->
<script src="{{ asset('vendor/js/jquery.min.js') }}"></script>
<!-- DataTables JS - Local -->
<script src="{{ asset('vendor/js/jquery.dataTables.min.js') }}"></script>

<script>
$(document).ready(function() {
    // Prevent any accidental form submissions on the page
    $('body').on('keypress', 'input:not([type="submit"])', function(e) {
        if (e.keyCode === 13) {
            e.preventDefault();
            return false;
        }
    });

    // Check if table has no rows
    const hasNoRows = $('#usersTable tbody tr td[colspan]').length > 0;
    if (hasNoRows) {
        return;
    }
    
    // Check if DataTable already exists and destroy it
    if ($.fn.DataTable.isDataTable('#usersTable')) {
        $('#usersTable').DataTable().destroy();
    }
    
    // Initialize DataTables (matching academic management style)
    const table = $('#usersTable').DataTable({
        paging: false,
        info: false,
        lengthChange: false,
        order: [[0, 'asc']],
        columnDefs: [
            { orderable: false, targets: -1 }
        ],
        dom: 'frt',
        language: {
            search: "_INPUT_",
            searchPlaceholder: "Search users..."
        }
    });

    // Move search to filters container and style it
    $('.dataTables_filter').prependTo('#filtersContainer');
    $('.dataTables_filter input').addClass('w-64 pr-3 py-2 border border-gray-300 rounded-lg text-sm placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 transition-colors font-normal').css('padding-left', '2.5rem');
    $('.dataTables_filter').addClass('relative flex items-center').prepend('<svg class="absolute left-4 h-4 w-4 text-gray-400 pointer-events-none" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"/></svg>');

    // Select all checkbox functionality
    function initializeSelectAll() {
        $('#selectAll').off('change').on('change', function() {
            $('.user-checkbox:visible').prop('checked', this.checked);
        });

        $('.user-checkbox').off('change').on('change', function() {
            const total = $('.user-checkbox:visible').length;
            const checked = $('.user-checkbox:visible:checked').length;
            $('#selectAll').prop('checked', total === checked && total > 0);
        });
    }

    initializeSelectAll();

    // Force status filter to "All Status" on page load (ignore URL parameters)
    $('#statusFilter').val('');

    // Apply initial role filter from URL if it exists (but not status)
    {% if filters.role %}
        {% if filters.role == 1 %}
            $('#roleFilter').val('Administrator').trigger('change');
        {% elseif filters.role == 2 %}
            $('#roleFilter').val('Department Head').trigger('change');
        {% elseif filters.role == 3 %}
            $('#roleFilter').val('Faculty').trigger('change');
        {% endif %}
    {% endif %}

    // Role filter - search by role text
    $('#roleFilter').on('change', function() {
        const roleText = $(this).val();
        table.column(5).search(roleText).draw();
    });

    // Status filter - search by status text
    $('#statusFilter').on('change', function() {
        const statusText = $(this).val();
        table.column(-2).search(statusText).draw();
    });

    // College filter - filters departments AND table by college
    $('#collegeFilter').on('change', function() {
        const collegeId = $(this).val();
        filterDepartmentsByCollege();
        
        if (collegeId) {
            // Get all departments for this college
            const collegeDepts = [];
            $('#departmentFilter option').each(function() {
                const deptCollegeId = $(this).data('college-id');
                if (deptCollegeId == collegeId) {
                    collegeDepts.push($(this).text().trim());
                }
            });
            
            // Filter table to show only users from departments in this college
            if (collegeDepts.length > 0) {
                const searchPattern = collegeDepts.join('|');
                table.column(-3).search(searchPattern, true, false).draw();
            } else {
                table.column(-3).search('').draw();
            }
        } else {
            // Clear department filter when "All Colleges" is selected
            $('#departmentFilter').val('');
            table.column(-3).search('').draw();
        }
    });

    // Department filter - search by department text
    $('#departmentFilter').on('change', function() {
        const deptText = $(this).find('option:selected').text().trim();
        if ($(this).val() && deptText !== 'All Departments') {
            table.column(-3).search('^' + deptText + '$', true, false).draw();
        } else {
            // If "All Departments" selected but college is still selected, reapply college filter
            const collegeId = $('#collegeFilter').val();
            if (collegeId) {
                $('#collegeFilter').trigger('change');
            } else {
                table.column(-3).search('').draw();
            }
        }
    });

    // Clear all filters
    $('#clearFilters').on('click', function() {
        // Reset all dropdowns to default (check if they exist first)
        if ($('#roleFilter').length) {
            $('#roleFilter').val('');
        }
        $('#statusFilter').val('');
        if ($('#collegeFilter').length) {
            $('#collegeFilter').val('');
        }
        if ($('#departmentFilter').length) {
            $('#departmentFilter').val('');
        }
        
        // Clear all DataTables filters
        table.search('').columns().search('').draw();
        $('.dataTables_filter input').val('');
        
        // Reset department dropdown to show all departments
        if (typeof filterDepartmentsByCollege === 'function') {
            filterDepartmentsByCollege();
        }
    });
});

// Filter departments by selected college
function filterDepartmentsByCollege() {
    const collegeSelect = document.querySelector('select[name="college_id"]');
    const deptSelect = document.querySelector('select[name="department_id"]');
    
    if (!collegeSelect || !deptSelect) return;
    
    const selectedCollegeId = collegeSelect.value;
    const deptOptions = deptSelect.querySelectorAll('option:not(:first-child)');
    
    // Store current department selection
    const currentDeptValue = deptSelect.value;
    let currentDeptStillVisible = false;
    
    // Show/hide departments based on college
    deptOptions.forEach(option => {
        if (!selectedCollegeId || option.dataset.collegeId === selectedCollegeId) {
            option.style.display = '';
            if (option.value === currentDeptValue) {
                currentDeptStillVisible = true;
            }
        } else {
            option.style.display = 'none';
        }
    });
    
    // Reset department selection if current selection is no longer visible
    if (!currentDeptStillVisible && currentDeptValue) {
        deptSelect.value = '';
    }
}

// Bulk actions
function bulkAction(action) {
    const checkboxes = document.querySelectorAll('.user-checkbox:checked');
    if (checkboxes.length === 0) {
        alert('Please select at least one user.');
        return;
    }

    const userIds = Array.from(checkboxes).map(cb => cb.value);
    const actionText = action === 'delete' ? 'delete' : action;
    
    if (!confirm(`Are you sure you want to ${actionText} ${userIds.length} selected user(s)?`)) {
        return;
    }

    fetch('{{ path('admin_api_users_bulk_action') }}', {
        method: 'POST',
        headers: {
            'Content-Type': 'application/json',
            'X-Requested-With': 'XMLHttpRequest'
        },
        body: JSON.stringify({
            action: action,
            user_ids: userIds
        })
    })
    .then(response => response.json())
    .then(data => {
        if (data.success) {
            location.reload();
        } else {
            alert('Error: ' + data.message);
        }
    })
    .catch(error => {
        console.error('Error:', error);
        alert('An error occurred while performing the bulk action.');
    });
}

// Initialize on page load
document.addEventListener('DOMContentLoaded', function() {
    // Filter departments based on selected college on page load
    filterDepartmentsByCollege();
    
    // Bulk Actions Dropdown - Pure JavaScript Implementation
    const bulkActionsContainer = document.getElementById('bulkActionsContainer');
    const bulkActionsToggle = document.getElementById('bulkActionsToggle');
    const bulkActionsArrow = document.getElementById('bulkActionsArrow');
    const bulkActionsMenu = document.getElementById('bulkActionsMenu');
    
    let isBulkActionsOpen = false;
    
    // Function to show bulk actions dropdown
    function showBulkActionsDropdown() {
        if (isBulkActionsOpen) return;
        
        isBulkActionsOpen = true;
        bulkActionsMenu.classList.remove('hidden');
        bulkActionsArrow.classList.add('rotate-180');
        
        // Trigger animation
        setTimeout(() => {
            bulkActionsMenu.classList.remove('opacity-0', 'scale-95');
            bulkActionsMenu.classList.add('opacity-100', 'scale-100');
        }, 10);
    }
    
    // Function to hide bulk actions dropdown
    function hideBulkActionsDropdown() {
        if (!isBulkActionsOpen) return;
        
        isBulkActionsOpen = false;
        bulkActionsMenu.classList.remove('opacity-100', 'scale-100');
        bulkActionsMenu.classList.add('opacity-0', 'scale-95');
        bulkActionsArrow.classList.remove('rotate-180');
        
        // Hide after animation
        setTimeout(() => {
            bulkActionsMenu.classList.add('hidden');
        }, 200);
    }
    
    // Function to toggle bulk actions dropdown
    function toggleBulkActionsDropdown(event) {
        event.preventDefault();
        event.stopPropagation();
        
        if (isBulkActionsOpen) {
            hideBulkActionsDropdown();
        } else {
            showBulkActionsDropdown();
        }
    }
    
    // Add click handler to bulk actions toggle
    if (bulkActionsToggle) {
        bulkActionsToggle.addEventListener('click', toggleBulkActionsDropdown);
    }
    
    // Close bulk actions dropdown when clicking outside
    document.addEventListener('click', function(event) {
        if (!isBulkActionsOpen) return;
        
        // Check if click is inside the bulk actions container
        if (!bulkActionsContainer.contains(event.target)) {
            hideBulkActionsDropdown();
        }
    });
    
    // Enhanced delete confirmation
    const deleteButtons = document.querySelectorAll('button[title="Delete"]');
    deleteButtons.forEach(function(button) {
        button.addEventListener('click', function(e) {
            e.stopPropagation();
            if (!confirm('⚠️ Are you sure you want to delete this user?\n\nThis action cannot be undone and will permanently remove:\n• User account and profile\n• All associated data\n• Access permissions\n\nClick OK to proceed or Cancel to abort.')) {
                e.preventDefault();
                return false;
            }
        });
    });

});
</script>
{% endblock %}

{% endblock %}