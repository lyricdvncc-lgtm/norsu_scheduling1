[build]
builder = "nixpacks"
watchPatterns = ["**/*.php", "composer.json", "composer.lock"]

[deploy]
startCommand = "bash -c 'php -S 0.0.0.0:${PORT:-8000} -t public'"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
healthcheckPath = "/"
healthcheckTimeout = 100
